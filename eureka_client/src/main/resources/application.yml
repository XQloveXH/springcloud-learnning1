server:
  port: 9000
spring:
  application:
    name: spring-cloud-producer
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8001/eureka/
management:
  endpoints:
    web:
      exposure:
        include: "*"

